<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.udacity.shoestore.screens.shoe_details.ShoeDetailFragment">

<!--    The constraint layout allow in this case place the views in a convenient way, every
one is related to each other. This layout fits to most of the mobile devices. -->

<!--    In the rubrics says to do the following: Detail screen uses the <data> element.
        I think that in my case it is not useful because in this fragment the UI doesn't hold
        any data, it just take the information about the new shoe and sends it to the list screen.
        The list screen does need to implement the <variable> <data> ... schema because it holds the
        shoe list, and when this one changes, the changes must be reflected in the UI. The
        textView's strings of the details screen are saved as resources as they are static.

        Another approach is to pass the List<Shoe> from the list screen to the details screen and
        then the new shoe is added to the list of shoe variable and later sends it back to the list
        screen with the new shoe inside the list. This is done by using safeArgs which I already use
        in the first scope. If you want me to implement this approach, please let me know.

        I would appreciate any suggestion to improve the app and the most important thing to learn
        more about Android. Thank you. -->

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">


        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginStart="16dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            app:layout_constraintBottom_toTopOf="@+id/cancel_button"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/textView4"
                    style="@style/textViewStyle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/shoe_name" />

                <EditText
                    android:id="@+id/shoe_name_editt"
                    style="@style/textViewInputs"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:hint="@string/shoe_name"
                    android:inputType="text" />

                <TextView
                    android:id="@+id/textView5"
                    style="@style/textViewStyle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="@string/company" />


                <EditText
                    android:id="@+id/company_editt"
                    style="@style/textViewInputs"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:hint="@string/company"
                    android:inputType="text" />

                <TextView
                    android:id="@+id/textView6"
                    style="@style/textViewStyle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="@string/size" />

                <EditText
                    android:id="@+id/size_editt"
                    style="@style/textViewInputs"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:hint="@string/size"
                    android:inputType="number" />

                <TextView
                    android:id="@+id/textView7"
                    style="@style/textViewInputs"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="@string/description" />


                <EditText
                    android:id="@+id/description_editt"
                    style="@style/textViewInputs"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="@string/description"
                    android:inputType="textMultiLine" />


            </LinearLayout>
        </ScrollView>


        <Button
            android:id="@+id/save_button"
            style="@style/next"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:paddingStart="2dp"
            android:paddingEnd="2dp"
            android:text="@string/save"
            android:theme="@style/next"
            app:layout_constraintBottom_toBottomOf="@+id/cancel_button"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toEndOf="@+id/cancel_button"
            app:layout_constraintTop_toTopOf="@+id/cancel_button" />

        <Button
            android:id="@+id/cancel_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginBottom="24dp"
            android:text="@string/cancel"
            android:theme="@style/cancel_button"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/save_button"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toStartOf="parent" />


    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>